/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['storage.googleapis.com', 'media.autochek.africa', 'www.w3schools.com'],
    },
}

module.exports = nextConfig
